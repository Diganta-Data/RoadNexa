services:
  - type: web
    name: roadnexa-backend
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: BACKEND_HOST
        value: 0.0.0.0
      - key: BACKEND_RELOAD
        value: "false"
      - key: LOG_LEVEL
        value: INFO
      - key: UPLOAD_DIR
        value: /tmp/roadnexa/uploads
      - key: CORS_ORIGINS
        sync: false
      - key: SECRET_KEY
        generateValue: true
